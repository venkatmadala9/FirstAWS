pipeline {
    agent any
    tools {
        maven "maven"
        jdk "Java"
    }
    stages {
        stage('Initialize'){
            steps{
                echo "PATH = ${M2_HOME}/bin:${PATH}"
                echo "M2_HOME = /opt/maven"
            }
        }
        stage('Build') {
            steps {
                dir("/Users/Welcome/git/repository/FirstAWS") {
                bat 'mvn -B -DskipTests clean package'
                }
            }
        }
     }
    
}
